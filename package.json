{
  "name": "hide-hate-backend",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "-------↓開発環境----": "",
    "ローカルWebサーバ": "functions-framework --target hello --debug",
    "ローカルfirestore": "firebase emulators:start --import=emulator_data --export-on-exit",
    "-------↓バッチ----": "",
    "ローカルモデル作成": "npx env-cmd -f batch/.env.batch.dev python batch/create_model.py"
  },
  "devDependencies": {
    "env-cmd": "^10.1.0",
    "npm-run-all": "^4.1.5"
  }
}
